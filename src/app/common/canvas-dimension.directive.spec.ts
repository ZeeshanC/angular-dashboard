import { CanvasDimensionDirective } from './canvas-dimension.directive';

describe('CanvasDimensionDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasDimensionDirective();
    expect(directive).toBeTruthy();
  });
});
